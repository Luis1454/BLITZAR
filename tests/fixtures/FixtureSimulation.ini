# Clean-room fixture for the legacy directive-file syntax.
simulation(particle_count=4, dt=0.01, solver=direct, integrator=leapfrog)
object(id=first, name="First")
object(id=second, name="Second")
generation(seed=42, deterministic=true)
metadata(note="")
